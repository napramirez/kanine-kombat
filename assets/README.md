Place the licensed title-screen music file here as:

- `assets/title-screen-bgm.mp3`

Place the in-fight background music file here as:

- `assets/fight-bgm.mp3`

Place the character-select background music file here as:

- `assets/char-select-bgm.mp3`

The title-screen track will loop on the title screen and stop when the player moves to character select.

The character-select track will loop during fighter selection and stop when the match begins.

The fight track will loop during rounds and restart from the beginning every time a new round starts.

The requested track name for this slot is:

- `In the Beginning 2021 Remaster Mortal Kombat Arcade OST`

This repository does not include the audio file itself.
